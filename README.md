## 3D mobile-friendly cozy library slice-of-life game with trust-based dialogue, mini-games, customization, and multiple endings

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
